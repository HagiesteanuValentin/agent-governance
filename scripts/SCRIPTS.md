# Scripts — agent-governance

One line per script; the scripter keeps it current; the orchestrator reads it before a scripter brief.

| script | what it does | files touched | arguments | adaptation: easy/medium/hard — what changes | one-off/reusable |
| --- | --- | --- | --- | --- | --- |
| `scripts/verifica-debloat.sh` | checks the sizes, forbidden strings, and keywords in the CLAUDE.md templates + the orchestration injection in the hook | `templates/CLAUDE.global.md`, `templates/orchestrare.md`, `hooks/session-start.sh` | none | easy — change the thresholds and string lists at the top of the script | reusable |
| `scripts/cell-setup.sh` + `scripts/cell-teardown.sh` | creates/deletes 6 worktrees (cells; base commit via `BASE=<sha>`, default master) from the `blueprint_pictura` parent, symlinks node_modules, optional dossier, `npm run check` baseline | `blueprint_pictura` (worktree add/remove, read-only on the parent) | `<task> <run> [--dosar DIR]` / `<task> <run> --yes` | easy — add/remove cells from the `CELLS` array at the top of the script | reusable |
| `scripts/cell-brief.sh` | writes the per-cell briefs from a template: replaces `__ROOT__` with the cell's worktree | writes `metrics-local/experiments/<task>/input/<template>.<cell>-<run>.md` (overwrites) | `<task> <run> <template>` | easy — same `CELLS` array as in `cell-setup.sh`; the worktrees' path is `EXPERIMENTE` at the top of the script | reusable |
| `tools/cell_metrics.py` | per-run cell metrics, in context bands, aggregated | read-only; writes `cells.md`/`cells.json` + the reports | `--subagents-dir` (repeatable) `--agent-prefix` `--results-dir` `--dump-reports` `--md/--json/--out-dir` | easy — `BANDS` and the metric lists at the top of the script | reusable |
| `scripts/verifica-simplu.mjs` | the verifier the cells read: only items I1–I8 | read-only on the worktree; writes only `--json` | `<root> [--json <out>]` | medium — each item I is a separate block; exports `creeazaContext`/`itemiI` | reusable |
| `scripts/evalueaza-simplu.mjs` | the orchestrator's evaluator: imports the I items and adds T1–T5 + silent deletions | read-only on the worktree; writes only `--json` | `<root> [--report <md>] [--json <out>]` | medium — the T traps are tied to specific files/patterns | reusable |
| `scripts/dosar-simplu.sh` | generates the distractor dossier (v0 brief, trimmed old versions, check, report) | writes only into `<out-dir>`; reads the parent with `git show` | `[out-dir] [--recheck]`, env `MAMA` `MAX_LINII` | easy — change the `SURSE` array (nn:label:version:hash:path) and `MAX_LINII` | reusable |
| `hooks/agenti-vii.sh` + `hooks/test-agenti-vii.sh` | live-agent cap per session: start/stop/check, ask at >=4 | `/tmp/claude-hooks/live-<session_id>` (state only) | `start\|stop\|check`, env `AGENTI_VII_CAP` `AGENTI_VII_STALE` `CLAUDE_HOOKS_DIR` | easy — the cap and staleness come from env | reusable |
| `hooks/context-agent.sh` + `hooks/test-context-main.sh` | context budget for main and for agents, verification counter | only `/tmp/claude-hooks` (markers + log) | `--scope main\|agent` `--warn N` `--deny N` `--any-type` `--praguri-tip` | easy — `TYPE_LIMITS`, `AGENT_PREFIXES`, `SAFE_SUBAGENTS`, `PLAN_DIR` at the top of the script | reusable |
| `hooks/commit-gate.sh` + `hooks/test-commit-gate.sh` | ask on `git commit` touching .ts/.js/.mjs/.astro without a CONFORM audit | `/tmp/claude-hooks/audit-ok-<session_id>` (read-only) | `--extensii ts,tsx,js,jsx,mjs,astro`, env `CLAUDE_HOOKS_DIR` `COMMIT_GATE_GIT` | easy — extensions from the argument, the reason from `REASON` | reusable |
| `tools/session_metrics.py --build-effort-baseline <project>` | writes `tools/effort-baseline.json` (n=403 Fable high turns, median output 502, thinking 129) | `tools/effort-baseline.json` (overwrites) | `--build-effort-baseline ~/.claude/projects/<project>` | easy — rebuilt on a new model | reusable |
| `scripts/reanalyze-metrics.sh` | reruns `session_metrics.py` on each record's transcript in `metrics-local/`, so it picks up new fields | `metrics-local/*.json` (overwrites), reads `~/.claude/projects/*/<session>.jsonl` | `[--dry-run] [--limit N] DIR` | easy — `PROJECTS_DIR` and the excluded names (`pending-rating.json`, `cells*.json`) at the top of the script | reusable |
| `scripts/install-autonom.sh` | installs the autonomous mode on the live setup: cp `autonom.sh`, a hunk in `agenti-vii.sh`, 2 entries in `settings.json` (backup first), then verifies (json, grep=2, code diff, test 15/15) | `~/.claude/hooks/autonom.sh`, `~/.claude/hooks/agenti-vii.sh`, `~/.claude/settings.json` | `bash scripts/install-autonom.sh` (`LIVE=<dir>` for a dry run); run by the user, the classifier blocks agents on `~/.claude` | easy — idempotent | reusable on re-sync |
| `scripts/pricing-cache-5m.py` | writes prices into pricing.json + 5m/1h cache-write billing in the analyzer | `tools/pricing.json`, `tools/session_metrics.py` | `--pricing --metrics --price NAME=in,out,cread,cw5m,cw1h (repeatable) --note --date [--only F] [--dry-run]` | easy — prices come from `--price`; the 12 code anchors live in `RULES`, tested by `scripts/fixtures/pricing-cache-5m/test.sh` | one-off (rerunnable, idempotent) |
| `scripts/check-pointers.sh` | checks that every PATTERNS/RECIPES section pointer in the repo matches a real heading in the target doc | read-only | none | easy — the exempt placeholder list inside the script | reusable |
| `easy_install.sh` | one-shot install of agents/hooks/commands/skills + settings.json, CLAUDE.md, orchestrare*.md into `$HOME/.claude`, with timestamped backup and restore | `~/.claude/{agents,hooks,commands,skills}`, `~/.claude/{settings.json,CLAUDE.md,orchestrare*.md}`, writes `~/.claude-backup-<ts>/` | `[--dry-run] [--yes] [--restore <backup-dir>]` | easy — the copied lists (`INSTALLED_DIRS`, `INSTALLED_FILES`) are at the top of the script | reusable (idempotent) |
</content>
| `scripts/export-public.sh` | exports the public whitelist into a target dir, verifies it, commits | writes only into `<target>` (wipes it except `.git`), read-only on the source | `<target> [--dry-run] [--no-git]` | easy — `WHITELIST` and `CITE_SOURCES` arrays at the top of the script | reusable (idempotent) |
| `scripts/verifica-complex.mjs` | verifier for the "complex" cell experiment (items I1–I5: price parsing, pretRon/pretEstimat in the generated file, scoring line unchanged, email suffix, `--verifica` exit 0) | read-only on the root (generator runs in a tmp dir) | `<root> [--json <out>]` | medium — slug lists and expected counts are hardcoded at the top of the script | reusable |
